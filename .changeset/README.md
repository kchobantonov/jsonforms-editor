# Releases

Run `pnpm changeset` for a user-facing package change and commit the generated file.
On master, Changesets opens or updates a release PR. Merging it publishes the
versioned packages to npm and creates GitHub releases and tags.
See [release setup](../docs/releases.md) for repository configuration and validation.
